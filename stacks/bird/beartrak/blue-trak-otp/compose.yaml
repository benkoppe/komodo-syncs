services:
  blue-trak-otp:
    extends:
      file: /var/lib/komodo/repos/trak-backend-bird/otp/compose.yaml
      service: otp
    container_name: blue-trak-otp
    networks:
      - cloudflared
    env_file:
      - .env

networks:
  cloudflared:
    external: true
